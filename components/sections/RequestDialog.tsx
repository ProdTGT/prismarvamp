export default function RequestDialog() {
  return (
      <dialog id="request-dialog" className="site-dialog" aria-labelledby="request-title"><button className="dialog-close" aria-label="Close request" data-close>×</button><div className="eyebrow" id="request-kicker">LET’S TALK ABOUT YOUR SETUP</div><h2 id="request-title">Request a 15-minute call.</h2><p className="dialog-lead" id="request-description">Share a preferred time. The team will confirm availability with you.</p><form id="request-form"><div className="form-grid"><label>Your name<input name="name" autoComplete="name" required maxLength={100} placeholder="Full name" /></label><label>Business name<input name="business" autoComplete="organization" required maxLength={150} placeholder="Your business" /></label></div><label id="request-timing">Preferred day, time & time zone<input name="timing" maxLength={150} placeholder="e.g. Tuesday, 2 pm Eastern" /></label><label id="request-context-label"><span id="request-context-title">Anything you’d like to discuss? (optional)</span><textarea name="context" rows={3} maxLength={1500} placeholder="Tell us about your setup…"></textarea></label><button className="button" type="submit" id="request-submit">Prepare my call request <span aria-hidden={true}>↗</span></button><p className="form-note" id="request-footnote">You’ll see a confirmation page next. Your call is confirmed only when the team replies.</p><p className="request-status" id="request-status" role="status" hidden></p><a id="request-fallback" className="text-link" hidden>Open my email draft ↗</a></form><a className="dialog-phone text-link" href="tel:+17074398264">Or call +1 (707) 439-8264 <span aria-hidden={true}>↗</span></a></dialog>
  );
}
